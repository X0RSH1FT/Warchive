# Data

This area is for machine-readable artifacts.

- `sources/` stores raw imports.
- `processed/` stores normalized outputs and generated artifacts.
- `manifests/` stores indexes and catalogs that describe the archive.
