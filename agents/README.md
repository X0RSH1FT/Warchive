# Agents

This area stores reusable machine-facing assets.

- `prompts/` holds prompt fragments and reusable instructions.
- `playbooks/` holds stepwise task procedures.
- `personas/` holds optional role-specific operating modes.
- `evaluations/` holds tests, expected outputs, or review notes.
