# Playbooks

Use this directory for workflows an agent can execute repeatedly with minimal ambiguity.

Good playbooks are:

- scoped to one outcome
- explicit about inputs and outputs
- clear about trust boundaries
- easy to review and revise

Start with `templates/playbook.md` when adding a new workflow.
